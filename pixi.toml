[project]
name = "defectmof"
version = "0.1.0"
description = "MOF defect supercell generation and simulation"
channels = ["conda-forge", "pytorch"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.11"

[pypi-dependencies]
defectmof = { path = ".", editable = true }
pytest = "*"
ruff = "*"
